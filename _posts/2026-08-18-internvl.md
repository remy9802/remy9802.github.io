---
title: "文献笔记｜InternVL：扩展视觉基础模型并对齐通用视觉语言任务"
date: 2026-08-18
permalink: /posts/internvl/
tags: [literature-note, vision-language-model, internvl, multimodal-llm]
note_type: single-paper
literature_topics: [vlm]
---

> **阅读范围**：阅读全文，包括三阶段训练、表征任务、对话评价与连接器消融。  
> **检索日期**：2026-08-18。  
> **主题**：当 LLM 已大规模扩展时，同步扩大视觉编码器和跨模态中间层能带来什么？

## 文献档案

- **文献链接**：[CVPR 2024 正式页面](https://openaccess.thecvf.com/content/CVPR2024/html/Chen_InternVL_Scaling_up_Vision_Foundation_Models_and_Aligning_for_Generic_CVPR_2024_paper.html)
- **代码链接**：[OpenGVLab/InternVL](https://github.com/OpenGVLab/InternVL)
- **作者 / 机构 / 年份**：Zhe Chen、Jiannan Wu、Wenhai Wang、Weijie Su、Guo Chen、Sen Xing、Muyan Zhong、Qinglong Zhang、Xizhou Zhu、Lewei Lu、Bin Li、Ping Luo、Tong Lu、Yu Qiao、Jifeng Dai；OpenGVLab / Shanghai AI Laboratory、Nanjing University、The University of Hong Kong、The Chinese University of Hong Kong、Tsinghua University、University of Science and Technology of China、SenseTime Research；2024。
- **出版状态**：CVPR 2024，页 24185-24198；DOI：不可用（CVF 页面未提供）。

## 核心结论

InternVL 选择与 LLaVA 相反的扩展方向：构建约 5.9B 的视觉编码器和约 8B 的 QLLaMA 中间件，同时保留对比学习与生成学习。结果支持“大视觉模型 + 重型连接器”能改善多类任务，但训练/推理成本、数据规模和见过测试标注的问题同样突出。

## 检索记录

- **数据源**：CVF Open Access、官方 GitHub。
- **检索式**：`InternVL scaling vision foundation model QLLaMA InternViT 6B CVPR 2024`。
- **纳入原因**：代表同步扩展视觉编码器和跨模态中间件、兼容表征与生成任务的路线。
- **版本核验**：阅读 CVPR 正式开放版本；代码仓库与论文首页一致。

## 研究问题

许多 VLM 使用大语言模型、较小视觉编码器和轻量投影，可能导致视觉容量与语言容量失衡。InternVL 研究：把视觉编码器扩展到 6B，并使用带 cross-attention 的语言中间件对齐，是否能同时服务分类、检索、caption、VQA 和对话？

## 方法与数据

InternViT-6B 约 5.9B 参数，结构细节见论文表 1。QLLaMA 从多语言 LLaMA-7B 初始化，增加 cross-attention 和 96 个 learnable queries；整体约 8B，其中新增跨模态参数约 1B。

训练分三阶段：第一阶段在从 6.03B 原始图文对清洗出的 4.98B 数据上做对比学习；第二阶段精选 1.03B 数据，联合 image-text contrastive、matching 和 generation；第三阶段使用约 400 万条 caption、VQA、grounding、OCR、对话等指令数据连接 Vicuna/InternLM（表 2、3）。模型可单独使用 InternViT 做感知，也可用 InternVL-C/G 做检索和生成，或连接 LLM 做聊天。

## 主要发现

1. **视觉编码器本身具备广泛迁移能力。** InternViT-6B 在线性分类、ADE20K 分割和多种 ImageNet 变体上取得有竞争力的结果（表 4、5），支持扩大视觉模型能改善通用表征。
2. **QLLaMA 强化跨模态对齐。** InternVL-G 在中英文图文检索上通常高于只用 InternViT 的 InternVL-C（表 7），生成式 caption 与 VQA 也从重型中间件受益。
3. **聊天指标有提升，但成本很高。** 完整 QLLaMA + Vicuna-13B 报告 VQAv2 81.2、MME 1586.4、POPE 87.6；同一 InternViT-6B 配轻量 MLP + Vicuna-13B 为 80.2、1546.9、87.1（表 9）。提升存在，但并非所有指标都大幅变化。
4. **部分结果不是严格 zero-shot。** 表 9 用 `*` 标记训练时已经观察相应数据集标注的结果，包括完整模型的部分 caption/VQA 项，不能与真正未见标注的基线等价解读。

## 论文结论

作者把 InternVL 描述为视觉语言领域的“瑞士军刀”：一个大视觉基础模型经过渐进式对齐后，可以在对比表征、生成任务和多模态对话之间复用，并成为 ViT-22B 等大视觉模型的替代路线。

## 局限与适用边界

- 视觉编码器约 5.9B、QLLaMA 约 8B，再连接 7B/13B LLM，训练和部署成本远高于轻量 projector 路线。
- 数据达到数十亿图文对，清洗、去重和潜在 benchmark 污染无法完整独立审计。
- 表 9 多项结果见过训练标注，不宜概括为全面 zero-shot 优势。
- 论文跨分类、检索、VQA、聊天报告大量指标，却缺少统一算力/延迟比较，无法判断参数效率。
- 本文是原始 InternVL，不能代表后续 InternVL2、2.5、3 的当前能力。

## 我的思考

InternVL 提醒我们，VLM 的视觉侧不能永远只是冻结的“小配件”。但它也把核心问题从“性能够不够”推向“额外 10B 级视觉与中间件参数值不值得”。更有说服力的后续实验应在固定总参数、视觉 token、显存和延迟下比较重型 QLLaMA 与轻量 MLP，而不只是比较最高准确率。

## 参考文献

1. Chen, Z., Wu, J., Wang, W., Su, W., Chen, G., Xing, S., Zhong, M., Zhang, Q., Zhu, X., Lu, L., Li, B., Luo, P., Lu, T., Qiao, Y., & Dai, J. (2024). *InternVL: Scaling up Vision Foundation Models and Aligning for Generic Visual-Linguistic Tasks*. CVPR, 24185-24198. [正式页面](https://openaccess.thecvf.com/content/CVPR2024/html/Chen_InternVL_Scaling_up_Vision_Foundation_Models_and_Aligning_for_Generic_CVPR_2024_paper.html) · [代码](https://github.com/OpenGVLab/InternVL) · DOI：不可用（CVF 记录未提供）。
